#include "gtest/gtest.h"

namespace {
  using ::testing::Test;

  namespace log_server_construction {

    // Should actually compile and pass a test
    TEST( LogServerConstruction,
          ShouldActuallyCompileAndPassATest ) {
      EXPECT_TRUE( true );
    }
    
  }
}
